<p>
  <strong>Role Name: </strong><%= @role.name %><br>
  <strong>Applications: </strong><%= safe_join(@role.software_apps.map { |l| link_to l.name, software_app_path(l.id) }, ', '.html_safe) %><br>
</p>

<hr>

Nodes belonging to <%= @role.name %>:<br>

<p>
<table class="table table-hover">
  <tr>
    <th>Name</th>
    <th colspan="3"></th>
  </tr>

  <% @role.nodes.map.each do |node| %>
    <tr>
      <td><%= link_to node.name, node_path(node.id) %><br></td>
    </tr>
  <% end %>
</table>
</p>


<%= link_to 'Edit', edit_role_path(@role) %> |
<%= link_to 'Back to Role List', roles_path %>
