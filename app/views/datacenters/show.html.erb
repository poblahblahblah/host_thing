<p>
  <strong>Datacenter Name: </strong><%= @datacenter.name %><br>
  <strong>Datacenter Vendor: </strong><%= @datacenter.vendor %><br>
  <strong>Datacenter Provider: </strong><%= @datacenter.provider %><br>
</p>

<hr>

Nodes Belonging to <%= @datacenter.name %>:<br>

<p>
<table class="table table-hover">
  <tr>
    <th>Name</th>
    <th colspan="3"></th>
  </tr>

  <% Node.where(datacenter_id: @datacenter.id).ids.each do |node_id| %>
    <tr>
      <td><%= link_to Node.find(node_id).name, node_path(Node.find(node_id).name) %><br></td>
    </tr>
  <% end %>
</table>
</p>

<%= link_to 'Edit', edit_datacenter_path(@datacenter) %> |
<%= link_to 'Back to Datacenter List', datacenters_path %>
