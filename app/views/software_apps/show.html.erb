<p>
  <strong>Software Application Name: </strong><%= @software_app.name %><br>
  <strong>Role Memberships: </strong><%= safe_join(@software_app.roles.map { |l| link_to(Role.find(l.id).name, role_path(l.id)) }, ', '.html_safe) %><br>
</p>

<hr>

Nodes belonging to <%= @software_app.name %>:<br>

<p>
<table class="table table-hover">
  <tr>
    <th>Name</th>
    <th colspan="3"></th>
  </tr>

  <% @software_app.nodes.map.each do |node| %>
    <tr>
      <td><%= link_to node.name, node_path(node.id) %><br></td>
    </tr>
  <% end %>
</table>
</p>

<%= link_to 'Edit', edit_software_app_path(@software_app) %> |
<%= link_to 'Back to Software Application List', software_apps_path %>
