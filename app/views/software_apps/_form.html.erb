<%= form_with model: @software_app, local: true do |form| %>

  <% if @software_app.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@software_app.errors.count, "error") %> prohibited
        this software_app from being saved:
      </h2>
      <ul>
        <% @software_app.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= form.label :name %><br>
    <%= form.text_field :name %>
  </p>

  <p>
    <%= form.label :roles %><br>
    <%= form.collection_check_boxes(:roles, Role.all, :id, :name) %>
  </p>

  <p>
    <%= form.submit %>
  </p>

<% end %>
