<h1>Roles</h1>

<table class="table table-hover">
  <tr>
    <th>Name</th>
    <th>Applications</th>
    <th>Actions</th>
    <th colspan="4"></th>
  </tr>

  <% @roles.each do |role| %>
    <tr>
      <td><a href="roles/<%= role.id %>"><%= role.name %></a></td>
      <td><%= safe_join(role.software_apps.map { |l| link_to(SoftwareApp.find(l.id).name, software_app_path(l.id)) }, ', '.html_safe) %></td>
      <td>
        <%= link_to 'Show', role_path(role) %> |
        <%= link_to 'Edit', edit_role_path(role) %> |
        <%= link_to 'Destroy', role_path(role), method: :delete, data: { confirm: 'Are you sure?' } %>
      </td>
    </tr>
  <% end %>
</table>

<%= link_to 'New Role', new_role_path, class: "btn btn-primary" %>
