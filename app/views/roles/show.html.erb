<p>
  <strong>Name:</strong>
  <%= @role.name %>
</p>

<hr>

Nodes belonging to <%= @role.name %>:<br>

<p>
<table class="table table-hover">
  <tr>
    <th>Name</th>
    <th colspan="3"></th>
  </tr>

  <% @role.nodes.map.each do |node| %>
    <tr>
      <td><%= link_to Node.find(node.id).name, node_path(Node.find(node.id).id) %><br></td>
    </tr>
  <% end %>
</table>
</p>


<%= link_to 'Edit', edit_role_path(@role) %> |
<%= link_to 'Back', roles_path %>
